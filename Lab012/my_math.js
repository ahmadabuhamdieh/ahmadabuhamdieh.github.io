module.exports.add = function (x, y) {
  return x + y;
};

module.exports.subtract = function (x, y) {
  return x - y;
};

module.exports.multiply = function (x, y) {
  return x * y;
};

module.exports.divide = function (x, y) {
  return x / y;
};

module.exports.pi = 3.14;

/*
function addV2(x, y) {
  return x + y;
}

function subtractV2(x, y) {
  return x - y;
}
*/
